# AI Accountability Agent

A React-based AI agent that tracks tasks and gives strict productivity feedback.

## Setup

1. Install dependencies:
npm install

2. Add your API key in `.env`:
VITE_GROQ_API_KEY=your_key_here

3. Run:
npm run dev
